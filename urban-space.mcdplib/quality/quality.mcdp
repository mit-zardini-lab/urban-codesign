dp {

    provides accessibility [dimensionless]
    provides greenery [dimensionless]
    provides approval [dimensionless]

    requires layout [`layout]

    implemented-by yaml resource("quality-assess.dpc.yaml")
}